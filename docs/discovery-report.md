# Core SDK Discovery Report — auth0-auth-java

> **Generated by:** SDK Wrapper Skill, Stage 1
> **Date:** 2026-02-24

## SDK Identity

- **Name:** auth0-auth-java
- **Repository:** Local workspace (see ./auth0-auth-java)
- **Language:** Java
- **Build System:** Gradle (multi-module)
- **License:** MIT
- **Minimum Java Version:** Java 11+

## API Surface

- **Main JWT validation entry point:** `com.auth0.validators.JWTValidator`
  - `validateToken(String token, Map<String, String> headers, HttpRequestInfo httpRequestInfo)`
  - `validateTokenWithRequiredScopes(...)`, `validateTokenWithAnyScope(...)`
  - `validateTokenWithClaimEquals(...)`, `validateTokenWithClaimIncludes(...)`, `validateTokenWithClaimIncludesAny(...)`
  - `decodeToken(String token)`
- **Supporting classes:**
  - `com.auth0.models.AuthOptions` (domain, audience)
  - `com.auth0.jwk.JwkProvider` (JWKS retrieval)
  - `com.auth0.jwt.JWTVerifier`, `com.auth0.jwt.interfaces.DecodedJWT`
  - `com.auth0.validators.ClaimValidator` (scope/claim checks)
- **Error Handling:**
  - All validation methods throw `BaseAuthException` or subclasses (`MissingRequiredArgumentException`, `VerifyAccessTokenException`)
- **Sync/Async Model:**
  - All methods are synchronous (blocking)
- **Configuration:**
  - Requires Auth0 domain and audience (via `AuthOptions`)
  - JWKS URL inferred from domain
- **Dependencies:**
  - `com.auth0:java-jwt`
  - `com.auth0:jwks-rsa`
  - Java 11+

## Test Patterns

- Unit tests for validators and orchestrators (see `src/test`)
- Exception handling and negative test cases

## Notes

- The SDK is modular: JWT validation is in `auth0-api-java` submodule.
- No async/reactive APIs; all validation is blocking.
- Designed for extensibility (custom `JwkProvider` can be injected).

---

_This report is based on local analysis of the `auth0-auth-java` workspace. See also the example report in `docs/examples/auth0-auth-java-discovery-report.md`._
